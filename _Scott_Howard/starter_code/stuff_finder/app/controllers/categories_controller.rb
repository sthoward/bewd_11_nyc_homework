class CategoriesController < ApplicationController

	def create
		# safe_category = params.require(:category).permit(:name)
		# classified_id = params[:classified_id]
		# classified = Classified.find(classified_id)
		# @category.classified = classified
		# @category.save
		# redirect_to
	end


end
